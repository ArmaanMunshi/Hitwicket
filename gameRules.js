// gameRules.js (Shared Game Rules)
const boardSize = 5;

function validateMove(character, move, gameState) {
    // Implement the logic to validate the move based on character type
    // Ensure the move is within bounds and doesn't involve friendly fire
    // Return { valid: true/false, newState }
}

module.exports = { validateMove };
